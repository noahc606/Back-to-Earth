#pragma once
#include <map>
#include <vector>
#include <SDL2/SDL.h>

class Defs
{
public:
    typedef long long t_ll;
    typedef std::map<int, std::vector<SDL_Texture*>> t_textureMap;

protected:

private:
};
